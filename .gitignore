# local secrets — never commit
.env

# python
__pycache__/
*.pyc

# NOTE: seen.json is intentionally NOT ignored — the workflow commits it back
# each run so dedup state survives between ephemeral GitHub Actions runners.
