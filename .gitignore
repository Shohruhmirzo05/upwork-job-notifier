# local secrets — never commit
.env
.DS_Store

# dedup state — lives in the DigitalOcean service state directory
seen.json

# cached visitor token (runtime only)
.token.json

# python
__pycache__/
*.pyc

# on-demand proposal job store (runtime only)
store.json

# manual proposal QA artifacts
proposal-qa-output/

# Cloudflare tracker build/runtime + local credentials
tracker/node_modules/
tracker/dist/
tracker/.wrangler/
.tracker-credentials

# local Product Design QA captures (report stays versioned)
tracker/design-qa-*.png
